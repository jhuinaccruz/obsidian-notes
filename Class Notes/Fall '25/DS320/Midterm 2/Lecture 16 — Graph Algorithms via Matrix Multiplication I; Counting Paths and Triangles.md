__Matrix Multiplication can solve__: Graph problems
- Runtime of the multiplication of square matrices is `M(n)`

__An adjacency matrix that has no weights and is undirected, without any self loops or parallel edges has/is__: 
- Symmetry
$$
A_{ij} = A_{ji}
$$
- Binary: Every entry is either a 0 or 1
- Zeros (on the diagonal)

