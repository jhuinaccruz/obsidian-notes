__Topological Ordering__: In a graph
$$
G = (V, E)
$$
a permutation
$$
\sigma = (v_1, v_2, ..., v_n) \in V
$$
can be a topological ordering of G if for every directed edge `(u, v) ∈ E`, `u` appears before `v` in  `σ`

__Directed Acrylic Graph__ (DAG): A directed graph without a cycle