__Gradient Descent__: An algorithm that numerically estimates where a function outputs its lowest values by finding the local minima to 0 without setting the gradient to 0, such that it approximates the solution
$$
x_n+1 = n_n - \alpha\nabla f(x_n)
$$
where
- alpha: The step size/learning rate