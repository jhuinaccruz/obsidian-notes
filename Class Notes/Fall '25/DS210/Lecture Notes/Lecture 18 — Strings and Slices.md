__Slice__: A reference to a contiguous portion of data without ownership
- They are "fat pointers", meaning they are a pointer that also contain length

__Unicode__: A standard that assigns a unique number to every character across all writing systems, giving four bytes per character

__UTF-8__: An encoding of Unicode, where each character gets 1-4 bytes per character, backwards compatible with ASCII

