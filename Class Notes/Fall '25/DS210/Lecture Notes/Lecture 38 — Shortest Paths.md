__Dijkstra's Algorithm__: A greedy algorithm for finding shortest paths, where the closest unvisited vertices are visited, then its neighbors distances are updated
- Time: `O(E log V)`
- Space: `O(V)`
